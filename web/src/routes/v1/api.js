export const api = {
	local: 'http://localhost:8000',
	allMakes: '/vehicle/makes',
	modelsByMake: '/vehicle/models', //vehicle/models/${make}
	modelsByMakeAndYear: '/vehicle/models', ///vehicle/models/${make}/${year}
};
